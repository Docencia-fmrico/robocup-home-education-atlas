<root main_tree_to_execute = "MainTree">
     <BehaviorTree ID="MainTree">
        <ReactiveSequence name="root_sequence">
            <DetectOperator name="detect_operator" new_goal="{new_goal}" position="{position}"/>
            <GoToPosition  name="move" new_goal="{new_goal}" position="{position}"/>
        </ReactiveSequence>
    </BehaviorTree>
 </root>